@page
@model ScholarlySoftwareSearch.Pages.Softwares.IndexModel

@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>

<form>
    <p>
        <select asp-for="SoftwareTag" asp-items="Model.Tags">
            <option value="">All</option>
        </select>
        Title: <input type="text" asp-for="SearchString" />
        <input type="submit" value="Filter" />
    </p>
</form>


<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Author(s)
            </th>
            <th>
                Upload Date
            </th>
            <th>
                Publisher
            </th>
            <th>
                Tag
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Software)
        {
        <tr>
            <td><a asp-page="./Details" asp-route-id="@item.Id">
                @Html.DisplayFor(modelItem => item.SoftwareName)
            </a></td>
            <td>
                @Html.DisplayFor(modelItem => item.Authors)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UploadDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Publisher)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tag)
            </td>
            <td>

                @if (SignInManager.IsSignedIn(User) && User.IsInRole("Manager"))
                {
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a>
                }
                @if (SignInManager.IsSignedIn(User) && (User.IsInRole("Admin") || item.UploaderID == User.Identity.Name))
                {
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a>
                    <a>|</a>
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                }
            </td>
        </tr>
        }
    </tbody>
</table>
